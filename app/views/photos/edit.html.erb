<h1>Editing photo</h1>
</br>
Previous Photo:
</br>
</br>

<img src= <%= "/data/"+@photo.id.to_s+@photo.url %>/>
</br>
<%= render 'form' %>

<%= link_to 'Show', users_path+"/"+@photo.user_id.to_s+"/photos/"+@photo.id.to_s %> |
<%= link_to 'Back', users_path+"/"+@photo.user_id.to_s+"/photos/" %>
