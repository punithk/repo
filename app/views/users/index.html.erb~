<h1>LIST OF ALL USERS</h1>

 <%= form_tag( search_path , method: "get") do %>
      <%= label_tag(:q, "Search for:") %>
      <%= text_field_tag(:q) %>
      <%= submit_tag("Search") %>
    <% end %>
  </br>
  </br>  
<table >
  <tr >
    <th>UID</th>
    <th>FIRST NAME</th>
    <th>LAST NAME</th>
    <th>INFORMATION</th>
    <th>ACTIONS</th>
  </tr>
<% @users.each do |user| %>
  <tr >
    <td><%= user.id %></td>
    <td><%= user.fname %></td>
    <td><%= user.lname %></td>
    <td><%= link_to 'Photos', user_path(user)+"/photos" %>|<%= link_to 'Profile', user_path(user)+"/profiles" %></td>
    <td><%= link_to 'Show', user %>|<%= link_to 'Edit', edit_user_path(user) %>|<%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
<br />
Create New User by clicking 

<%= link_to 'here', new_user_path %>

